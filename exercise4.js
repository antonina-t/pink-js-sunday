// Welcome to the fourth exercise - here we practise creating new arrays using existing ones.

const arr1 = [1,2,3];
const arr2 = [4,5,6];
// Set a to [1,2,3,4,5,6] by combining arr1 and arr2 using concat
const a = undefined;
console.log(`a should be [1,2,3,4,5,6]: ${JSON.stringify(a)}`);

// Set b to [1,2,3,4,5,6,7] by combining arr1, arr2 and number 7 using the spread syntax
const b = undefined;
console.log(`b should be [1,2,3,4,5,6,7]: ${JSON.stringify(b)}`);

// Set c to an array where each element is taken from arr1 and multiplied by 2, using map
const c = undefined;
console.log(`c should be [2,4,6]: ${JSON.stringify(c)}`);

const words = ["hello", "this", "is", "a", "pink", "workshop"];
// Set d to an array containing lengths of words in the words array, using map
const d = undefined;
console.log(`d should be [5,4,2,1,4,8]: ${JSON.stringify(d)}`);

const numbers = [0,5,6,3,-6,10,100,2,-1000];
// Set e to an array containing only positive numbers from the numbers array, using filter
const e = undefined;
console.log(`e should be [5,6,3,10,100,2]: ${JSON.stringify(e)}`);

// Set f to an array containing only negative numbers from the numbers array, using filter
const f = undefined;
console.log(`f should be [-6,-1000]: ${JSON.stringify(f)}`);